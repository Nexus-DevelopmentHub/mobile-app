import 'package:flutter/material.dart';

class PageDetailPlayer extends StatefulWidget {
  const PageDetailPlayer({Key? key}) : super(key: key);

  @override
  State<PageDetailPlayer> createState() => _PageDetailPlayerState();
}

class _PageDetailPlayerState extends State<PageDetailPlayer> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
